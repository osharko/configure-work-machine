# Env vars per default app (lette al login da systemd --user).

# Editor: neovim ovunque (vim → nvim alias)
EDITOR=nvim
VISUAL=nvim
SUDO_EDITOR=nvim
GIT_EDITOR=nvim
MANPAGER=nvim +Man!

# Browser
BROWSER=brave

# Pager
PAGER=less
